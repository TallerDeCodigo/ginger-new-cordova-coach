	
	hola
	{{# is_read}}
		<img class="icon-small" src="{{cordova_full_path}}images/read.jpg" alt="" id="read_{{../_id}}">
	{{/ is_read}}
	
	{{# is_delivered}}
		<img class="icon-small" src="{{cordova_full_path}}images/delivered.jpg" alt="" id="delivered_{{../_id}}">
	{{/ is_delivered}}

	{{# isMessageSticker}}
		<div class="message-sticker-container"></div>
	{{/ isMessageSticker}}
	
	{{!-- var sender = (localStorage.getItem('idSender') == messageSenderId)?'outgoing':'incoming'; --}}
	{{# items}}
		<div class="list-group-item" id="{{_id}}" onclick="clickToAddMsg('{{_id}}')">'+
			<div class="{{sender}}">
				item
				<p>
					{{# attachmentFileId}}
						<a href="http://api.quickblox.com/blobs/'+attachmentFileId+'/download.xml?token='+token+'">
							<img src="http://api.quickblox.com/blobs/'+attachmentFileId+'/download.xml?token='+token+'" alt="attachment" class="attachments img-responsive" />
						</a>
					{{/ attachmentFileId}}
					{{# messageAttach}}
						{{.}}
					{{/ messageAttach}}
					{{message}}
				</p>
			</div>
		</div>
		<div class="clearfix"></div>
	{{/ items}}